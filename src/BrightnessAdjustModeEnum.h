#ifndef BRIGHTNESS_ADJUST_MODE_ENUM_H
#define BRIGHTNESS_ADJUST_MODE_ENUM_H

enum UserBrightnessAdjustMode {
  ubamCancel,
  ubamMin,
  ubamMax,
  ubamDaylightBoost
};

#endif // BRIGHTNESS_ADJUST_MODE_ENUM_H
